import React from "react";

const SubmitButton = () => {
    return (
        <button className={'submitButton'} disabled>
            Submit Artwork
        </button>
    )
}

export default SubmitButton;